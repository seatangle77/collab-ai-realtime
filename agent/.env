# 数据库（SSH 隧道转发到本地 15432）
DB_HOST=127.0.0.1
DB_PORT=15432
DB_NAME=collaborative_ai_chatbot
DB_USER=app_user
DB_PASSWORD=StrongPass123

# NLP 微服务（本地 backend）
NLP_BASE_URL=http://localhost:8000

# NLP 接口鉴权（与 backend ADMIN_API_KEY 一致）
ADMIN_TOKEN=TestAdminKey123

# 定时器间隔（毫秒）
SHORT_INTERVAL_MS=30000
LONG_INTERVAL_MS=120000
SESSION_POLL_MS=15000
