# 密钥统一放根目录 secrets.env，这里只放非敏感配置

# 数据库连接（密码在 secrets.env）
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=collaborative_ai_chatbot
DB_USER=app_user

# NLP 微服务地址（admin token 在 secrets.env）
NLP_BASE_URL=http://localhost:8000

# 定时器间隔（毫秒）
SHORT_INTERVAL_MS=30000
LONG_INTERVAL_MS=120000
SESSION_POLL_MS=15000
